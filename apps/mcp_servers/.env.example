# =============================================================================
# MCP Servers Configuration
# =============================================================================

# =============================================================================
# Composio Integration
# =============================================================================

# Composio API Key (get from https://app.composio.dev)
COMPOSIO_API_KEY=your_composio_api_key_here

# Callback URL scheme for OAuth redirects
COMPOSIO_CALLBACK_SCHEME=jarvis://

# =============================================================================
# Unified MCP Server (Claude Code + Playwright)
# =============================================================================

# Server Configuration
MCP_SERVER_PORT=8932
MCP_SERVER_HOST=localhost

# Claude CLI Configuration
# Override the Claude CLI binary name or path (default: claude)
CLAUDE_CLI_NAME=claude
# Enable debug logging for Claude Code operations
MCP_CLAUDE_DEBUG=false

# Playwright Configuration
# Browser to use: chromium, firefox, webkit, msedge
PLAYWRIGHT_BROWSER=chromium
# Run browser in headless mode (default: false for headed/visible)
PLAYWRIGHT_HEADLESS=false
# Path to persistent browser profile directory
PLAYWRIGHT_USER_DATA_DIR=./browser-profile
# Action timeout in milliseconds
PLAYWRIGHT_TIMEOUT_ACTION=5000
# Navigation timeout in milliseconds
PLAYWRIGHT_TIMEOUT_NAVIGATION=60000
# Viewport size (widthxheight)
PLAYWRIGHT_VIEWPORT_SIZE=1280x720

# Extension Support (connect to existing browser)
# Enable connection to existing browser via Playwright MCP Bridge extension
PLAYWRIGHT_EXTENSION_ENABLED=true
# CDP endpoint for connecting to existing browser
PLAYWRIGHT_CDP_ENDPOINT=

# Logging
LOG_LEVEL=info
